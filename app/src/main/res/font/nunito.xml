<?xml version="1.0" encoding="utf-8"?>
<!--
    FONT SETUP INSTRUCTIONS
    =======================
    Download Nunito from Google Fonts:
    https://fonts.google.com/specimen/Nunito

    Required files — place in app/src/main/res/font/:
      nunito.xml            ← font family XML (this file, see below)
      nunito_regular.ttf    ← NunitoSans-Regular.ttf or Nunito-Regular.ttf
      nunito_semibold.ttf   ← Nunito-SemiBold.ttf
      nunito_bold.ttf       ← Nunito-Bold.ttf

    OR: use downloadable fonts in Android Studio:
    1. Right-click res/font → New → Font Resource File
    2. Search "Nunito" → select Regular, SemiBold, Bold
    3. Android Studio creates the XML automatically

    Font family XML (nunito.xml):
-->
<font-family xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <font
        android:font="@font/nunito_regular"
        android:fontStyle="normal"
        android:fontWeight="400"
        app:font="@font/nunito_regular"
        app:fontStyle="normal"
        app:fontWeight="400" />

    <font
        android:font="@font/nunito_semibold"
        android:fontStyle="normal"
        android:fontWeight="600"
        app:font="@font/nunito_semibold"
        app:fontStyle="normal"
        app:fontWeight="600" />

    <font
        android:font="@font/nunito_bold"
        android:fontStyle="normal"
        android:fontWeight="700"
        app:font="@font/nunito_bold"
        app:fontStyle="normal"
        app:fontWeight="700" />

</font-family>
